#include <stdio.h>

int main() {
    
    int i = 3;
    
    //Make the while loop until "i" is not higher than 15.
    while (i <= 15) {
     
     printf("%d ", i);
     i = i + 3;
    }
    return 0;
}

